<?xml version="1.0" encoding="UTF-8"?>
<prompt>
  <title>Master Prompt</title>

  <!-- Core Definitions -->
  <definitions>
    <def id="user_stories">All User Stories in spec.md</def>
    <def id="compush_cmd">/compush</def>
  </definitions>

  <!-- ========== LOCAL DEVELOPMENT WITH AZURE RESOURCES ========== -->
  <local_development type="REQUIRED">
    <mandate>Run all applications LOCALLY for development and testing, but connect to Azure resources.</mandate>

    <apps>
      <app name="SoCa">
        <backend>Port 8001 - uvicorn soca.main:app --port 8001</backend>
        <frontend>Port 5173 - npm run dev --port 5173</frontend>
      </app>
      <app name="Prompt Tuner">
        <backend>Port 8002 - uvicorn prompt_tuner.main:app --port 8002</backend>
        <frontend>Port 5174 - npm run dev --port 5174</frontend>
      </app>
    </apps>

    <azure_resources>
      <resource_group>ingen-test</resource_group>
      <cosmos_db>ingen-test-cosmos</cosmos_db>
      <storage>ingentestblob</storage>
      <tagging>Tag resources with "soca" or "prompt-tuner" as appropriate</tagging>
      <cost>Only provision the CHEAPEST possible resources needed</cost>
    </azure_resources>

    <workflow>
      <step>1. Start backend servers locally with Azure connection strings</step>
      <step>2. Start frontend dev servers locally</step>
      <step>3. Test using Playwright MCP against localhost URLs</step>
      <step>4. After code changes, restart local servers (no Azure deployment needed for testing)</step>
      <step>5. Run /compush only for final commit after validation passes</step>
    </workflow>
  </local_development>

  <!-- Primary Goal -->
  <goal>
    Work autonomously to validate the application end-to-end per all User Stories.
    Most implementation is complete - resolve all remaining issues until fully functional.
    YOU CANNOT STOP UNTIL EVERYTHING IN spec.md IS IMPLEMENTED AND VALIDATED.
  </goal>

  <!-- ========== AUTONOMOUS WORK EXPECTATIONS ========== -->
  <autonomous_work type="CRITICAL">
    <context_window_management>
      Your context window will be automatically compacted as it approaches its limit,
      allowing you to continue working indefinitely from where you left off.
    </context_window_management>

    <completion_mandate type="ABSOLUTE">
      <rule>YOU CANNOT STOP until EVERYTHING in spec.md is implemented and validated</rule>
      <rule>DO NOT stop early - work until EVERYTHING is FLESHED OUT COMPLETELY</rule>
      <rule>Do NOT stop tasks due to token budget concerns</rule>
      <rule>Complete tasks FULLY, even if end of budget is approaching</rule>
      <rule>NEVER artificially stop any task early regardless of context remaining</rule>
    </completion_mandate>

    <work_cycle type="REQUIRED">
      For EACH and EVERY feature in spec.md:
      <step>1. Validate using Playwright MCP</step>
      <step>2. Debug any issues found</step>
      <step>3. Code and implement fixes/features as needed</step>
      <step>4. Work to unblock and resolve ALL problems</step>
      <step>5. Re-validate until feature is fully functional</step>
      <step>6. Move to next feature - repeat until ALL features complete</step>
    </work_cycle>

    <expectation>
      CONTINUE and DO NOT STOP until the entire app is finished and validated end-to-end.
      You are expected to work autonomously for a VERY LONG PERIOD OF TIME to complete this task.
      The work is NOT complete until EVERY User Story in spec.md is:
      - Fully implemented
      - Thoroughly validated with Playwright MCP
      - Debugged and working correctly
      - Integrated and stable
    </expectation>

    <planning_approach>
      Be ambitious with task lists and planning - context management allows for extensive work sessions.
      Break down ALL User Stories comprehensively and work through EVERY SINGLE ONE without stopping.
    </planning_approach>
  </autonomous_work>

  <!-- ========== DEVELOPMENT WORKFLOW ========== -->
  <development_workflow>
    <code_changes>
      <trigger>After ANY code modification</trigger>
      <required_steps>
        <step>1. Run ${compush_cmd} immediately</step>
        <step>2. Confirm deployment successful</step>
        <step>3. Re-test functionality</step>
        <step>4. Proceed only after verifying stability</step>
      </required_steps>
    </code_changes>

    <debugging>
      <on_failure>Debug → Fix → ${compush_cmd} → Confirm resolution</on_failure>
      <persistence>Continue debugging and fixing until issue is COMPLETELY resolved</persistence>
    </debugging>
  </development_workflow>

  <!-- ========== VALIDATION & TESTING ========== -->
  <validation>
    <scope>
      <source>ALL User Stories in spec.md - EVERY SINGLE ONE must be validated</source>
      <aspects>Functional correctness, data consistency, state transitions, error handling</aspects>
    </scope>

    <method>
      <tool>Playwright MCP in integrated environment</tool>
      <requirement>Validate complete flows from start to expected result for EACH feature</requirement>
      <workflow>
        1. Use Playwright MCP to test feature
        2. If issues found: debug, code fixes, deploy with ${compush_cmd}
        3. Re-validate with Playwright MCP
        4. Repeat until feature is FULLY functional
        5. Move to next feature
      </workflow>
    </method>

    <page_requirements>
      <req>All functions fully implemented and data-driven (no hardcoded placeholders)</req>
      <req>Manual routes accessible via UI</req>
      <req>Document new components in @specs/ with behavior and state details</req>
      <req>Map UI elements to corresponding User Stories</req>
    </page_requirements>

    <completion_criteria>
      Work is NOT complete until:
      - Every feature in spec.md is implemented
      - Every feature passes Playwright MCP validation
      - No bugs or blockers remain
      - All flows work end-to-end
    </completion_criteria>
  </validation>

  <!-- ========== SPECIFICATIONS ========== -->
  <specs_compliance>
    <source>spec.md is the authoritative source - ALL items must be completed</source>
    <refinement_guideline>
      If specifications are unclear, refine them to be:
      - Explicit and structured
      - Consistent and agent-friendly
      - While preserving original intent
    </refinement_guideline>
  </specs_compliance>

  <!-- ========== EXECUTION PLAN ========== -->
  <execution_plan>
    <approach>
      Decompose ALL User Stories in spec.md into comprehensive, ambitious task list.
      Work through EVERY SINGLE task without stopping until complete.
      Context management supports extensive planning - be thorough and complete.
    </approach>

    <priorities>
      <priority level="1">Complete EVERYTHING in spec.md - no exceptions</priority>
      <priority level="2">Validate each feature with Playwright MCP</priority>
      <priority level="3">Debug and fix all issues immediately</priority>
      <priority level="4">Run ${compush_cmd} after each update</priority>
      <priority level="5">DO NOT STOP until all User Stories are done</priority>
    </priorities>

    <work_style>
      AUTONOMOUS execution WITHOUT STOPPING until ALL User Stories are:
      - Implemented completely
      - Validated end-to-end with Playwright MCP
      - Debugged and working correctly
      - Integrated and stable

      Work PERSISTENTLY for as long as needed. DO NOT stop early.
      EVERYTHING must be FLESHED OUT COMPLETELY before stopping.
    </work_style>
  </execution_plan>

  <!-- ========== FINAL REMINDER ========== -->
  <final_mandate type="ABSOLUTE">
    YOU CANNOT STOP WORKING UNTIL:
    - Every User Story in spec.md is implemented
    - Every feature is validated with Playwright MCP
    - Every bug is debugged and fixed
    - The entire application works end-to-end
    - EVERYTHING is FLESHED OUT COMPLETELY

    DO NOT STOP EARLY. WORK CONTINUOUSLY UNTIL COMPLETE.
  </final_mandate>
</prompt>
